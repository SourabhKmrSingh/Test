import React from 'react';
import {View, StyleSheet, Text} from 'react-native';

const Missed = () => {
    return (
        <View>
            <Text>This is Missed</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Missed;
